package com.sujood.app.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Color palette for Sujood app.
 * Inspired by Calm app with deep navy, soft purple, and warm amber accents.
 */

// Primary Background Colors
val DeepNavy = Color(0xFF0A0E1A)
val MidnightBlue = Color(0xFF111827)
val SurfaceDark = Color(0xFF1A1F36)

// Accent Colors
val SoftPurple = Color(0xFF6C63FF)
val WarmAmber = Color(0xFFF5A623)
val LavenderGlow = Color(0xFFA78BFA)
val SoftIndigo = Color(0xFF818CF8)

// Text Colors
val TextPrimary = Color(0xFFFFFFFF)
val TextSecondary = Color(0xFF8892A4)
val TextTertiary = Color(0xFF5A6275)

// Status Colors
val SuccessGreen = Color(0xFF4CAF50)
val WarningOrange = Color(0xFFFF9800)

// Gradient Colors for backgrounds
val GradientStart = Color(0xFF0A0E1A)
val GradientCenter = Color(0xFF151B2E)
val GradientEnd = Color(0xFF2D1F4E)

// Frosted Glass Colors
val GlassBackground = Color(0x20111827)
val GlassBorder = Color(0x40FFFFFF)

// Prayer-specific colors for visual variety
val FajrColor = Color(0xFFFFB74D)      // Warm orange for dawn
val DhuhrColor = Color(0xFF64B5F6)     // Blue for noon
val AsrColor = Color(0xFFBA68C8)       // Purple for afternoon
val MaghribColor = Color(0xFFFF8A65)  // Warm coral for sunset
val IshaColor = Color(0xFF7986CB)      // Indigo for night
